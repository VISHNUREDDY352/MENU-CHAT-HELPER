# Screenshots

Place your app screenshot here as `screenshot.png`

To add it:
1. Take a screenshot of the app (Win + Shift + S)
2. Save it as `screenshot.png` in this `docs/` folder
3. Run: `git add docs/screenshot.png && git commit -m "docs: add app screenshot" && git push`
